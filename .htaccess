# HSTS (HTTP Strict Transport Security) - Enforce HTTPS
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

# Enable HTTP Content-Type Sniffing Protection
Header set X-Content-Type-Options "nosniff"

# Prevent Clickjacking - X-Frame-Options
Header always set X-Frame-Options "DENY"

# Enable XSS Protection
Header set X-XSS-Protection "1; mode=block"

# Hide Server Version Data
ServerTokens Prod
ServerSignature Off

# Set MIME Types
AddType application/javascript .js
AddType text/css .css
AddType application/json .json
AddType text/html .html
AddType image/png .png
AddType image/jpeg .jpg .jpeg
AddType image/ico .ico
AddType image/gif .gif
AddType image/svg+xml .svg
AddType application/pdf .pdf
AddType video/webm .webm
AddType image/webp .webp

# Enable HSTS Preload (for adding your domain to the HSTS preload list)
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
